import { redirect } from "next/navigation";

/** Example remix removed — owned library + marketplace remix only. */
export default async function LegacyExampleRemixPage() {
  redirect("/marketplace");
}
